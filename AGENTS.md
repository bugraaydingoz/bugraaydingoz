# AGENTS

- Tech stack: TypeScript with pnpm
- Use Phosphor Icons from https://phosphoricons.com for UI icons via `@phosphor-icons/web` CSS classes
- Before committing, run `pnpm lint` and `pnpm test`
