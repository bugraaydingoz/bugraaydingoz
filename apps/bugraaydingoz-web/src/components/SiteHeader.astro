<header class="site-header">
    <a href="/" class="site-brand">
        <span>Buğra Aydıngöz</span>
        <span>Product Engineer</span>
    </a>
</header>

<style>
    .site-header {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 20px;
        margin-bottom: 72px;
    }

    .site-brand,
    .site-nav a {
        color: var(--foreground);
        text-decoration: none;
    }

    .site-brand {
        display: grid;
        gap: 7px;
        font-family:
            "Satoshi", Inter, "Inter Variable", Roboto, "Helvetica Neue", Arial,
            sans-serif;
    }

    .site-brand span:first-child {
        font-size: 19px;
        line-height: 1;
        font-weight: 600;
    }

    .site-brand span:last-child {
        color: var(--muted-foreground);
        font-size: 15px;
        line-height: 1;
        font-weight: 500;
    }

    .site-nav {
        display: flex;
        align-items: center;
        justify-content: flex-end;
    }

    .site-nav a {
        color: var(--muted-foreground);
        font-size: 16px;
        line-height: 1;
    }

    .site-nav a:hover,
    .site-nav a:focus-visible,
    .site-brand:hover span:first-child,
    .site-brand:focus-visible span:first-child {
        color: var(--primary);
    }

    @media (max-width: 720px) {
        .site-header {
            align-items: flex-start;
            flex-direction: column;
            gap: 24px;
            margin-bottom: 58px;
        }
    }
</style>
